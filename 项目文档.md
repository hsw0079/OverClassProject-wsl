# 学校车辆进出校园登记管理系统 — 开发文档

> Django 6.0 + simpleui + MySQL  
> 最后更新：2026-05-30

---

## 一、项目概述

本系统用于学校门卫/管理员对进出校园的车辆进行登记管理，涵盖**校内车辆档案**、**进出记录**、**访客预约**、**外来车辆档案**（月底自动清空）、**非机动车档案**，并提供统计看板和 CSV 导出功能。

---

## 二、创建 Django 项目

```bash
django-admin startproject OverClassProject .
```

---

## 三、安装依赖

```bash
pip install mysqlclient          # MySQL 驱动
pip install django-simpleui      # 后台主题
pip install whitenoise           # 静态文件托管
```

---

## 四、配置 settings.py

```python
import os
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent.parent

SECRET_KEY = 'your-secret-key-here'
DEBUG = True
ALLOWED_HOSTS = ['*']

# ---- 应用注册 ----
INSTALLED_APPS = [
    'simpleui',                              # 必须在 admin 之前
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'apps.vehicle_mgmt.apps.VehicleMgmtConfig',
]

# ---- 中间件 ----
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware',
]

# ---- simpleui 主题配置 ----
SIMPLEUI_HOME_ACTION = False        # 关闭广告
SIMPLEUI_HOME_QUICK = False         # 关闭快捷跳转
SIMPLEUI_HOME_INFO = False          # 关闭首页信息
SIMPLEUI_TABS = True                # 显示多选选项卡
SIMPLEUI_DEFAULT_COLLAPSED = False  # 侧边栏默认展开
SIMPLEUI_DEFAULT_THEME = 'Simpleui-x.css'
SIMPLEUI_HOME_PAGE = '/admin/stats/'  # 自定义首页 = 统计看板

# ---- 静态文件 ----
STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'
STATIC_URL = '/static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')

# ---- 媒体文件 ----
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'uploads')

# ---- 模板 ----
TEMPLATES = [{
    'BACKEND': 'django.template.backends.django.DjangoTemplates',
    'DIRS': [BASE_DIR / 'templates'],
    'APP_DIRS': True,
    'OPTIONS': {
        'context_processors': [
            'django.template.context_processors.request',
            'django.contrib.auth.context_processors.auth',
            'django.contrib.messages.context_processors.messages',
        ],
    },
}]

# ---- 数据库 ----
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'overclassproject',
        'USER': 'root',
        'PASSWORD': 'Abc123',
        'HOST': 'localhost',
        'PORT': 3306,
        'OPTIONS': {'init_command': 'SET default_storage_engine=InnoDB'},
    }
}

# ---- 国际化 ----
LANGUAGE_CODE = 'zh-hans'
TIME_ZONE = 'Asia/Shanghai'
USE_I18N = True
USE_TZ = True

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
```

---

## 五、创建数据库

```sql
CREATE DATABASE overclassproject CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

---

## 六、创建 App

```bash
mkdir apps
python manage.py startapp vehicle_mgmt apps/vehicle_mgmt
```

修改 `apps/vehicle_mgmt/apps.py`：

```python
from django.apps import AppConfig

class VehicleMgmtConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.vehicle_mgmt'
    verbose_name = '车辆进出管理系统'
```

---

## 七、配置 URL 路由

```python
# OverClassProject/urls.py
from django.contrib import admin
from django.urls import path, re_path
from django.views.static import serve
from django.shortcuts import redirect
from . import settings
from apps.vehicle_mgmt.views import stats_dashboard

urlpatterns = [
    path('', lambda request: redirect('admin:index')),
    path('admin/stats/', stats_dashboard, name='admin_stats'),  # 统计看板
    path('admin/', admin.site.urls),
    re_path(r'^media/(?P<path>.*)$', serve, {'document_root': settings.MEDIA_ROOT}),
]
```

---

## 八、数据模型

`apps/vehicle_mgmt/models.py`：

### 8.1 在校车辆档案 — SchoolVehicle

```python
class SchoolVehicle(models.Model):
    VEHICLE_TYPE_CHOICES = [
        ('school_bus', '校车'),
        ('staff', '教职工车辆'),
        ('official', '公务车辆'),
        ('visitor', '访客车辆'),
        ('delivery', '送货车辆'),
    ]
    plate_number = models.CharField('车牌号', max_length=20, unique=True)
    vehicle_type = models.CharField('车辆类型', max_length=20, choices=VEHICLE_TYPE_CHOICES, default='staff')
    brand = models.CharField('品牌', max_length=50, blank=True, null=True)
    color = models.CharField('颜色', max_length=20, blank=True, null=True)
    owner_name = models.CharField('所属人/部门', max_length=100)
    owner_phone = models.CharField('联系电话', max_length=20, blank=True, null=True)
    is_blacklisted = models.BooleanField('是否黑名单', default=False)
    blacklist_reason = models.TextField('拉黑原因', blank=True, null=True)
    remarks = models.TextField('备注', blank=True, null=True)
    created_at = models.DateTimeField('创建时间', auto_now_add=True)
    updated_at = models.DateTimeField('更新时间', auto_now=True)

    class Meta:
        verbose_name = '校内车辆档案'
        verbose_name_plural = verbose_name
        ordering = ['-created_at']
```

### 8.2 外来车辆档案 — ExternalVehicle

月底自动清空。当进出记录中填写了未在校内登记的车辆时，自动创建到此表。

```python
class ExternalVehicle(models.Model):
    plate_number = models.CharField('车牌号', max_length=20, unique=True)
    owner_name = models.CharField('所属人', max_length=100)
    owner_phone = models.CharField('联系电话', max_length=20, blank=True, null=True)
    created_at = models.DateTimeField('首次登记时间', auto_now_add=True)

    class Meta:
        verbose_name = '外来车辆档案'
        verbose_name_plural = verbose_name
        ordering = ['-created_at']
```

### 8.3 进出记录 — EntryExitRecord

核心模型，保存时自动关联车辆档案：

```python
class EntryExitRecord(models.Model):
    RECORD_TYPE_CHOICES = [('entry', '进校'), ('exit', '出校')]

    plate_number = models.CharField('车牌号', max_length=20)
    school_vehicle = models.ForeignKey(SchoolVehicle, on_delete=models.SET_NULL,
        null=True, blank=True, verbose_name='在校车辆', related_name='records')
    external_vehicle = models.ForeignKey(ExternalVehicle, on_delete=models.SET_NULL,
        null=True, blank=True, verbose_name='外来车辆', related_name='records')
    record_type = models.CharField('进出类型', max_length=10, choices=RECORD_TYPE_CHOICES)
    record_time = models.DateTimeField('进出时间', default=timezone.now)
    driver_name = models.CharField('驾驶员姓名', max_length=50)
    driver_phone = models.CharField('驾驶员电话', max_length=20, blank=True, null=True)
    passenger_count = models.PositiveIntegerField('同行人数', default=1)
    goods_info = models.TextField('货物信息', blank=True, null=True)
    purpose = models.TextField('事由', blank=True, null=True)
    expected_leave_time = models.DateTimeField('预计离校时间', blank=True, null=True)
    approver = models.CharField('审批人', max_length=50, blank=True, null=True)
    remarks = models.TextField('备注', blank=True, null=True)
    created_at = models.DateTimeField('登记时间', auto_now_add=True)

    def save(self, *args, **kwargs):
        self.plate_number = (self.plate_number or '').strip()
        sv = SchoolVehicle.objects.filter(plate_number=self.plate_number).first()
        if sv:
            self.school_vehicle = sv
            self.external_vehicle = None
        else:
            self.school_vehicle = None
            ev, _ = ExternalVehicle.objects.get_or_create(
                plate_number=self.plate_number,
                defaults={'owner_name': self.driver_name, 'owner_phone': self.driver_phone or ''}
            )
            self.external_vehicle = ev
        super().save(*args, **kwargs)
```

### 8.4 访客预约 — VisitorAppointment

```python
class VisitorAppointment(models.Model):
    STATUS_CHOICES = [
        ('pending', '待审批'), ('approved', '已通过'), ('rejected', '已拒绝'),
        ('arrived', '已到访'), ('left', '已离开'),
    ]
    visitor_name = models.CharField('访客姓名', max_length=50)
    visitor_phone = models.CharField('访客电话', max_length=20)
    plate_number = models.CharField('车牌号', max_length=20)
    host_name = models.CharField('被访人', max_length=50)
    host_department = models.CharField('被访部门', max_length=100, blank=True, null=True)
    expected_time = models.DateTimeField('预计进校时间')
    purpose = models.TextField('事由')
    status = models.CharField('状态', max_length=20, choices=STATUS_CHOICES, default='pending')
    approver = models.CharField('审批人', max_length=50, blank=True, null=True)
    remarks = models.TextField('备注', blank=True, null=True)
    created_at = models.DateTimeField('创建时间', auto_now_add=True)
    updated_at = models.DateTimeField('更新时间', auto_now=True)
```

### 8.5 非机动车档案 — NonMotorVehicle

独立管理，不关联进出记录。编号牌范围 001~999。

```python
import re
from django.core.exceptions import ValidationError

class NonMotorVehicle(models.Model):
    TYPE_CHOICES = [
        ('bicycle', '自行车'), ('ebike', '电动车'),
        ('tricycle', '三轮车'), ('other', '其他'),
    ]
    number_plate = models.CharField('编号牌', max_length=3, unique=True,
        help_text='范围 001~999，必须3位数字')
    vehicle_type = models.CharField('车辆类型', max_length=20, choices=TYPE_CHOICES, default='ebike')
    owner_name = models.CharField('所属人', max_length=100)
    owner_phone = models.CharField('联系电话', max_length=20, blank=True, null=True)
    brand = models.CharField('品牌', max_length=50, blank=True, null=True)
    color = models.CharField('颜色', max_length=20, blank=True, null=True)
    remarks = models.TextField('备注', blank=True, null=True)
    created_at = models.DateTimeField('创建时间', auto_now_add=True)
    updated_at = models.DateTimeField('更新时间', auto_now=True)

    def clean(self):
        super().clean()
        if not re.match(r'^(00[1-9]|0[1-9][0-9]|[1-9][0-9]{2})$', self.number_plate):
            raise ValidationError({'number_plate': '编号牌必须为 001~999 的3位数字'})
```

---

## 九、Admin 后台配置

`apps/vehicle_mgmt/admin.py`：

### 9.1 CSV 导出公用 Action

```python
import csv
from django.http import HttpResponse

def export_as_csv(model_admin, request, queryset):
    response = HttpResponse(content_type='text/csv')
    response['Content-Disposition'] = 'attachment; filename="export.csv"'
    response.charset = 'utf-8-sig'
    writer = csv.writer(response)
    field_names = [field.name for field in model_admin.model._meta.fields]
    writer.writerow(field_names)
    for obj in queryset:
        row = [str(getattr(obj, f) or '') for f in field_names]
        writer.writerow(row)
    return response

export_as_csv.short_description = '导出选中数据为 CSV'
```

### 9.2 在校车辆 Admin

```python
@admin.register(SchoolVehicle)
class SchoolVehicleAdmin(admin.ModelAdmin):
    list_display = ('plate_number', 'vehicle_type', 'owner_name', 'owner_phone',
                    'is_blacklisted', 'created_at')
    list_filter = ('vehicle_type', 'is_blacklisted', 'created_at')
    search_fields = ('plate_number', 'owner_name', 'owner_phone', 'brand')
    actions = [export_as_csv]

    fieldsets = (
        ('基本信息', {'fields': ('plate_number', 'vehicle_type', 'brand', 'color')}),
        ('所属信息', {'fields': ('owner_name', 'owner_phone')}),
        ('黑名单', {'fields': ('is_blacklisted', 'blacklist_reason')}),
        ('其他', {'fields': ('remarks',)}),
    )
```

### 9.3 外来车辆 Admin（含清空倒计时和清空按钮）

```python
@admin.register(ExternalVehicle)
class ExternalVehicleAdmin(admin.ModelAdmin):
    change_list_template = 'admin/external_vehicle_change_list.html'  # 自定义模板
    list_display = ('plate_number', 'owner_name', 'owner_phone', 'created_at')
    search_fields = ('plate_number', 'owner_name', 'owner_phone')
    actions = [export_as_csv, purge_external_vehicles_action]
```

### 9.4 进出记录 Admin（含自动关联）

```python
@admin.register(EntryExitRecord)
class EntryExitRecordAdmin(admin.ModelAdmin):

    def save_model(self, request, obj, form, change):
        # 显式执行自动关联逻辑
        obj.plate_number = (obj.plate_number or '').strip()
        sv = SchoolVehicle.objects.filter(plate_number=obj.plate_number).first()
        if sv:
            obj.school_vehicle = sv
            obj.external_vehicle = None
        else:
            obj.school_vehicle = None
            ev, _ = ExternalVehicle.objects.get_or_create(
                plate_number=obj.plate_number,
                defaults={'owner_name': obj.driver_name, 'owner_phone': obj.driver_phone or ''}
            )
            obj.external_vehicle = ev
        super().save_model(request, obj, form, change)

    list_display = ('plate_number', 'vehicle_source', 'record_type_display', 'record_time',
                    'driver_name', 'purpose_short', 'passenger_count', 'created_at')
    list_filter = ('record_type', 'record_time')
    search_fields = ('plate_number', 'driver_name', 'driver_phone', 'purpose')
    actions = [export_as_csv]

    def vehicle_source(self, obj):
        if obj.school_vehicle: return f'🏫 {obj.school_vehicle.owner_name}'
        if obj.external_vehicle: return '🚗 外来'
        return '—'
    vehicle_source.short_description = '车辆来源'
```

### 9.5 访客预约 Admin

```python
@admin.register(VisitorAppointment)
class VisitorAppointmentAdmin(admin.ModelAdmin):
    list_display = ('visitor_name', 'visitor_phone', 'plate_number', 'host_name',
                    'expected_time', 'status_display', 'approver', 'created_at')
    list_filter = ('status', 'expected_time', 'host_department')
    search_fields = ('visitor_name', 'visitor_phone', 'plate_number', 'host_name')
    actions = [export_as_csv]

    def status_display(self, obj):
        colors = {'pending': '🟡 待审批', 'approved': '🟢 已通过',
                  'rejected': '🔴 已拒绝', 'arrived': '🔵 已到访', 'left': '⚫ 已离开'}
        return colors.get(obj.status, obj.status)
    status_display.short_description = '状态'
```

### 9.6 非机动车 Admin

```python
@admin.register(NonMotorVehicle)
class NonMotorVehicleAdmin(admin.ModelAdmin):
    list_display = ('number_plate', 'vehicle_type', 'owner_name', 'owner_phone', 'created_at')
    list_filter = ('vehicle_type', 'created_at')
    search_fields = ('number_plate', 'owner_name', 'owner_phone')
    actions = [export_as_csv]
```

---

## 十、统计看板视图

`apps/vehicle_mgmt/views.py`：

```python
@staff_member_required
def stats_dashboard(request):
    today = now().date()

    today_entry_count = EntryExitRecord.objects.filter(
        record_type='entry', record_time__date=today).count()
    today_exit_count = EntryExitRecord.objects.filter(
        record_type='exit', record_time__date=today).count()

    entered_today = set(EntryExitRecord.objects.filter(
        record_type='entry', record_time__date=today
    ).values_list('plate_number', flat=True))
    exited_today = set(EntryExitRecord.objects.filter(
        record_type='exit', record_time__date=today
    ).values_list('plate_number', flat=True))
    on_campus_count = len(entered_today - exited_today)

    context = {
        'today_entry_count': today_entry_count,
        'today_exit_count': today_exit_count,
        'on_campus_count': on_campus_count,
        'total_vehicles': SchoolVehicle.objects.count(),
        'non_motor_count': NonMotorVehicle.objects.count(),
        'external_vehicles': ExternalVehicle.objects.count(),
        'pending_visitors': VisitorAppointment.objects.filter(status='pending').count(),
        'blacklisted_count': SchoolVehicle.objects.filter(is_blacklisted=True).count(),
        'today_arrived': VisitorAppointment.objects.filter(
            status__in=['arrived', 'left'], expected_time__date=today).count(),
        'recent_records': EntryExitRecord.objects.order_by('-record_time')[:10],
        'total_records_today': today_entry_count + today_exit_count,
    }
    return render(request, 'admin/stats_dashboard.html', context)
```

---

## 十一、模板

### 11.1 统计看板模板

`templates/admin/stats_dashboard.html` — 9 张数据卡片 + 最近进出记录表格，使用 CSS Grid 布局，响应式适配。

卡片展示内容：
- 📥 今日进校 / 📤 今日出校 / 🏫 当前在校
- 📋 在校车辆 / 🚲 非机动车 / 🚗 本月外来
- ⏳ 待审批访客 / 👤 今日到访 / 🚫 黑名单车辆

### 11.2 外来车辆清空倒计时模板

`templates/admin/external_vehicle_change_list.html` — 继承 `admin/change_list.html`，在内容顶部插入渐变色倒计时条：

- 背景：`linear-gradient(135deg, #f093fb, #f5576c)`（粉紫渐变）
- 倒计时数字：`font-size: 22px; font-weight: 700`，每 1 秒刷新
- 到期后显示"已到期，请清空！"
- 底部提供"⚠ 立即清空所有外来车辆记录" action 按钮

核心 JS：

```javascript
function getMonthEnd() {
    var now = new Date();
    return new Date(now.getFullYear(), now.getMonth() + 1, 0, 23, 59, 59);
}

function updateCountdown() {
    var diff = Math.floor((getMonthEnd() - new Date()) / 1000);
    if (diff <= 0) {
        document.getElementById('countdown-timer').textContent = '已到期，请清空！';
        return;
    }
    var days = Math.floor(diff / 86400);
    var hours = Math.floor((diff % 86400) / 3600);
    var mins = Math.floor((diff % 3600) / 60);
    var secs = diff % 60;
    document.getElementById('countdown-timer').textContent =
        days + '天 ' + pad(hours) + ':' + pad(mins) + ':' + pad(secs);
}
setInterval(updateCountdown, 1000);
```

---

## 十二、管理命令

### 12.1 播种测试数据

```bash
python manage.py seed_data
```

创建 3 辆在校车（含 1 辆黑名单）、2 辆外来车（自动创建）、4 条进出记录、2 条访客预约。

### 12.2 清空外来车辆档案

```bash
python manage.py purge_external_vehicles
```

删除 `ExternalVehicle` 表中所有记录。建议在每月月初执行。

---

## 十三、数据迁移 & 超级管理员

```bash
# 生成迁移文件
python manage.py makemigrations vehicle_mgmt

# 执行迁移
python manage.py migrate

# 创建超级管理员
python manage.py createsuperuser
```

默认管理员账户：`admin` / `admin123`

---

## 十四、收集静态文件

```bash
python manage.py collectstatic
```

---

## 十五、启动项目

```bash
python manage.py runserver
```

访问 `http://127.0.0.1:8000/admin/` 登录后台。

---

## 十六、用户角色权限说明

| 角色 | 权限范围 |
|------|----------|
| 管理员 | 外来车辆管理、校内车辆管理、访客预约管理、非机动车管理、用户组管理、用户管理 |
| 工作人员 | 外来车辆管理、校内车辆管理、访客预约管理、非机动车管理 |

> 注：当前版本所有用户均为超级管理员。角色权限控制可通过 Django 内置的 Group + Permission 体系实现。

---

## 十七、模块字段速查

| 模块 | 字段 |
|------|------|
| **在校车辆档案** | 车牌号、车辆类型、所属人/部门、联系电话、是否黑名单、创建时间 |
| **外来车辆档案** | 车牌号、所属人、联系电话、首次登记时间 |
| **进出记录** | 车牌号、车辆来源、进出类型、进出时间、驾驶员姓名、驾驶员电话、事由、同行人数、登记时间 |
| **访客预约** | 访客姓名、访客电话、车牌号、被访人、被访部门、预计进校时间、状态、审批人、创建时间 |
| **非机动车档案** | 编号牌(001~999)、车辆类型、所属人、联系电话、创建时间 |

---

## 十八、项目文件结构

```
OverClassProject/
├── manage.py
├── OverClassProject/
│   ├── settings.py          # 项目配置
│   ├── urls.py              # URL 路由
│   ├── wsgi.py / asgi.py
├── apps/
│   └── vehicle_mgmt/
│       ├── apps.py          # App 配置
│       ├── models.py        # 5 个数据模型
│       ├── admin.py         # Admin 注册 & 自动关联
│       ├── views.py         # 统计看板视图
│       ├── tests.py         # 27 个测试用例
│       ├── management/commands/
│       │   ├── seed_data.py           # 测试数据播种
│       │   └── purge_external_vehicles.py  # 外来车辆清空
│       └── migrations/
├── templates/admin/
│   ├── stats_dashboard.html                  # 统计看板
│   └── external_vehicle_change_list.html     # 外来车辆倒计时
├── staticfiles/
└── uploads/
```

---

## 十九、测试

```bash
python manage.py test apps.vehicle_mgmt.tests.AdminPageTests \
                       apps.vehicle_mgmt.tests.AutoLinkTests \
                       apps.vehicle_mgmt.tests.PurgeCommandTests \
                       apps.vehicle_mgmt.tests.NonMotorVehicleTests
```

**测试覆盖：27 项全部通过**

| 测试类 | 数量 | 覆盖内容 |
|--------|------|----------|
| AdminPageTests | 11 | 所有 Admin 列表页、搜索、筛选、黑名单、统计看板 |
| AutoLinkTests | 5 | 在校关联、外来创建、去重、在校优先、owner_name |
| PurgeCommandTests | 1 | 外来车辆清空命令 |
| NonMotorVehicleTests | 10 | 合法/非法编号牌、重复拦截、Admin 可访问、统计展示 |
