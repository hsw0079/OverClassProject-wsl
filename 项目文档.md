# 学校车辆进出校园登记管理系统 — 开发文档

> Django 6.0 + DRF + Vue 3/Vite + MySQL + openpyxl + qrcode
> 最后更新：2026-09-01

---

## 一、项目概述

本系统用于学校门卫/管理员对进出校园的车辆进行登记管理，涵盖**校内车辆档案**、**进出记录**、**访客预约**、**外来车辆档案**（月底自动清空）、**非机动车档案**、**二维码通行证**，并提供统计看板、CSV 导出和 Excel 批量导入功能。

系统是**双进程应用**：Django 后端（含 Admin 后台 + DRF API）在仓库根目录，Vue 3/Vite SPA 前端在 `frontend/`。用户通过 SPA 注册/登录、提交访客预约、查看审批状态、领取二维码通行证、使用校内导航。

---

## 二、技术栈

| 端 | 技术 |
|----|------|
| 后端 | Django 6.0.3、Django REST Framework、mysqlclient、whitenoise、openpyxl、qrcode、django-cors-headers |
| 前端 | Vue 3、Vite、Vue Router、Pinia、Axios |
| 数据库 | MySQL（`overclassproject`，utf8mb4） |
| 地图 | 高德地图 JS API（前端 SPA + Admin 地点选择器） |

> 说明：`django-simpleui` 虽列在 `requirements.txt`，但未加入 `INSTALLED_APPS`，实际未启用；Admin 样式由自定义 `static/css/admin_tesla.css` 提供。

---

## 三、项目结构

```
OverClassProject/
├── manage.py
├── requirements.txt
├── OverClassProject/           # 后端项目配置
│   ├── settings.py
│   ├── urls.py
│   ├── wsgi.py / asgi.py
├── apps/
│   ├── vehicle_mgmt/           # 车辆管理主业务
│   │   ├── models.py           # 6 个数据模型
│   │   ├── admin.py            # Admin 注册 & 自动关联 & CSV 导出
│   │   ├── views.py            # 统计看板 + Excel 导入 + 访客预约
│   │   ├── api_views.py        # DRF API（注册/预约/二维码/用户信息）
│   │   ├── serializers.py      # API 序列化器
│   │   ├── tests.py            # 27 个测试用例
│   │   ├── management/commands/
│   │   │   ├── seed_data.py               # 测试数据播种
│   │   │   └── purge_external_vehicles.py # 外来车辆清空
│   │   └── migrations/
│   └── campus_nav/             # 校园导航
│       ├── models.py           # CampusLocation
│       ├── admin.py            # Admin + 地图选点
│       ├── views.py            # CampusLocationViewSet
│       ├── serializers.py
│       ├── urls.py             # 挂载于 /api/admin/
│       └── migrations/
├── templates/                  # Django 模板（7 个自定义）
│   ├── visitor_appointment.html
│   └── admin/
│       ├── base_site.html
│       ├── stats_dashboard.html
│       ├── external_vehicle_change_list.html
│       ├── school_vehicle_change_list.html
│       ├── import_excel.html
│       └── campus_nav/change_list.html
├── static/                     # 源静态资源
│   ├── css/admin_tesla.css     # Admin Tesla 风格样式
│   ├── admin/js/               # 地图脚本
│   └── map/campus_map.jpg      # 校园地图占位图
├── staticfiles/                # collectstatic 输出
├── uploads/                    # 媒体文件根目录
└── frontend/                   # Vue 3 SPA
    └── src/
        ├── main.js
        ├── App.vue
        ├── api/index.js        # axios + Token 拦截器
        ├── stores/auth.js      # Pinia 认证 store
        ├── router/index.js     # 路由与登录守卫
        ├── assets/tesla.css    # Tesla 设计系统样式变量
        └── views/
            ├── Login.vue / Register.vue
            ├── Home.vue        # 预约申请/申请状态/校内导航
            ├── QRPass.vue      # 二维码通行证
            ├── Navigation.vue  # 地点列表
            └── NavigationDetail.vue  # 高德地图导航
```

---

## 四、后端配置要点（settings.py）

- `DEBUG = True`，`ALLOWED_HOSTS = ['*']`，`CORS_ALLOW_ALL_ORIGINS = True`（均为开发配置，勿用于生产）。
- 数据库：MySQL `overclassproject`（`localhost:3306`，`root`）。
- CSRF 中间件已注释（开发阶段禁用，前端 SPA 跨域请求）。
- `STATICFILES_STORAGE = 'whitenoise.storage.CompressedStaticFilesStorage'`；`STATIC_ROOT = staticfiles`，`STATICFILES_DIRS = [static]`；`MEDIA_ROOT = uploads`。
- DRF：
  - 认证：`TokenAuthentication`；权限：`IsAuthenticatedOrReadOnly`。
  - 分页：`PageNumberPagination`，`PAGE_SIZE = 20`。
- `ADMIN_HOME_REDIRECT = '/admin/stats/'`：登录后台后跳转统计看板。
- 语言 `zh-hans`，时区 `Asia/Shanghai`，`USE_TZ = True`。

---

## 五、URL 路由（OverClassProject/urls.py）

| 路径 | 说明 |
|------|------|
| `/` | 重定向到 Admin |
| `/appointment/` | 访客自助预约公开页 |
| `/api/auth/register/` | 用户注册（返回 token） |
| `/api/auth/login/` | 登录（DRF obtain_auth_token，CSRF 豁免） |
| `/api/auth/user/` | 当前用户信息 |
| `/api/appointments/` | 预约 CRUD（DRF Router，需登录） |
| `/api/appointments/<pk>/qr/` | 预约二维码图片 |
| `/api/appointments/<pk>/qr-info/` | 二维码令牌信息（有效期） |
| `/api/admin/locations/` | 校园地点 CRUD（campus_nav，挂载在 `api/admin/` 下） |
| `/admin/stats/` | 统计看板 |
| `/admin/import-excel/` | Excel 批量导入 |
| `/admin/download-template/` | 下载导入模板 |
| `/admin/` | Django Admin |
| `/media/<path>` | 媒体文件服务 |

> 已知问题：二维码图片内容指向 `/api/verify-qr/<token>/`，但该路由尚未在 `urls.py` 中注册。

---

## 六、数据模型（apps/vehicle_mgmt/models.py，共 6 个）

### 6.1 校内车辆档案 — SchoolVehicle

| 字段 | 类型 | 说明 |
|------|------|------|
| plate_number | CharField(20) | 车牌号，UNIQUE |
| vehicle_type | CharField(20) | choices: school_bus 校车 / staff 教职工 / official 公务 / visitor 访客 / delivery 送货，默认 staff |
| brand / color | CharField | 品牌 / 颜色，可空 |
| owner_name | CharField(100) | 所属人/部门，必填 |
| owner_phone | CharField(20) | 联系电话，可空 |
| is_blacklisted / blacklist_reason | BooleanField / TextField | 是否黑名单 / 拉黑原因 |
| remarks | TextField | 备注 |
| created_at / updated_at | DateTimeField | 创建/更新时间 |

### 6.2 外来车辆档案 — ExternalVehicle（月底自动清空）

| 字段 | 类型 | 说明 |
|------|------|------|
| plate_number | CharField(20) | 车牌号，UNIQUE |
| owner_name | CharField(100) | 所属人，必填 |
| owner_phone | CharField(20) | 联系电话，可空 |
| created_at | DateTimeField | 首次登记时间 |

当进出记录填写了未在校内登记的车牌时，保存时自动创建/关联到此表。

### 6.3 进出记录 — EntryExitRecord

| 字段 | 类型 | 说明 |
|------|------|------|
| plate_number | CharField(20) | 车牌号 |
| school_vehicle | FK(SchoolVehicle) | 在校车辆，SET_NULL，可空 |
| external_vehicle | FK(ExternalVehicle) | 外来车辆，SET_NULL，可空 |
| record_type | CharField(10) | entry 进校 / exit 出校 |
| record_time | DateTimeField | 进出时间，默认 now |
| driver_name / driver_phone | CharField | 驾驶员姓名 / 电话 |
| passenger_count | PositiveIntegerField | 同行人数，默认 1 |
| goods_info / purpose | TextField | 货物信息 / 事由 |
| expected_leave_time | DateTimeField | 预计离校时间 |
| approver | CharField(50) | 审批人 |
| remarks | TextField | 备注 |
| created_at | DateTimeField | 登记时间 |

**自动关联逻辑**：`save()` 会去除车牌号首尾空白；先查 `SchoolVehicle`，命中则关联在校车辆（清空外来关联），否则自动创建/关联 `ExternalVehicle`（owner 取驾驶员姓名/电话）。`apps/vehicle_mgmt/admin.py` 的 `EntryExitRecordAdmin.save_model` 复用了相同逻辑。

### 6.4 访客预约 — VisitorAppointment

| 字段 | 类型 | 说明 |
|------|------|------|
| user | FK(auth.User) | 关联用户，SET_NULL，可空（SPA 提交时关联） |
| visitor_name / visitor_phone | CharField | 访客姓名 / 电话 |
| plate_number | CharField(20) | 车牌号 |
| host_name / host_department | CharField | 被访人 / 被访部门 |
| expected_time | DateTimeField | 预计进校时间 |
| purpose | TextField | 事由 |
| status | CharField(20) | pending 待审批 / approved 已通过 / rejected 已拒绝 / arrived 已到访 / left 已离开 |
| approver | CharField(50) | 审批人 |
| remarks | TextField | 备注 |
| created_at / updated_at | DateTimeField | 创建/更新时间 |

### 6.5 非机动车档案 — NonMotorVehicle

| 字段 | 类型 | 说明 |
|------|------|------|
| number_plate | CharField(3) | 编号牌，UNIQUE，正则 `^(00[1-9]|0[1-9][0-9]|[1-9][0-9]{2})$`（001~999） |
| vehicle_type | CharField(20) | bicycle 自行车 / ebike 电动车 / tricycle 三轮车 / other 其他 |
| owner_name / owner_phone | CharField | 所属人 / 联系电话 |
| brand / color | CharField | 品牌 / 颜色 |
| remarks | TextField | 备注 |
| created_at / updated_at | DateTimeField | 创建/更新时间 |

### 6.6 二维码通行令牌 — QRPassToken

| 字段 | 类型 | 说明 |
|------|------|------|
| appointment | OneToOneField(VisitorAppointment) | 关联预约，CASCADE，反向名 `qr_token` |
| token | CharField(64) | 随机令牌，UNIQUE |
| expires_at | DateTimeField | 有效期至（生成时 +24h） |
| used_at | DateTimeField | 核销时间，可空 |
| created_at | DateTimeField | 创建时间 |

`generate_for_appointment()` 为已通过审批的预约生成 24 小时有效的令牌；`is_valid()` 判断未核销且未过期。

---

## 七、API 接口（apps/vehicle_mgmt/api_views.py）

- **POST `/api/auth/register/`**：`RegisterView`，注册后自动创建/返回 Token（`{"token", "user_id", "username"}`）。
- **POST `/api/auth/login/`**：`csrf_exempt_login`（DRF `obtain_auth_token`），用户名+密码换取 Token。
- **GET `/api/auth/user/`**：返回当前用户 `{id, username}`。
- **`/api/appointments/`**（`AppointmentViewSet`，需登录）：
  - GET 列表 / POST 创建（`user` 自动绑定当前用户）/ 详情 / 更新 / 删除。
  - 仅返回当前用户自己的预约。
- **GET `/api/appointments/<pk>/qr/`**：`appointment_qr`，仅已审批的预约可生成 PNG 二维码；令牌过期自动重发。
- **GET `/api/appointments/<pk>/qr-info/`**：`qr_token_info`，返回 `{token, expires_at, is_valid, remaining_seconds}`。
- **GET/POST `/api/admin/locations/`**（campus_nav）：校园地点列表/新增；读取公开，写入需认证（Token 或 Session）。

---

## 八、Admin 后台

### 8.1 CSV 导出通用 Action

`export_as_csv` 导出选中数据为 CSV（`utf-8-sig`），所有模块均挂载。

### 8.2 在校车辆 SchoolVehicleAdmin

- 自定义模板 `admin/school_vehicle_change_list.html`（黑名单提醒）。
- `list_display`：车牌号、类型、所属人、电话、是否黑名单、创建时间。
- 黑名单保存时自动补拉黑原因 `未填写原因`。
- 支持搜索、筛选、CSV 导出。

### 8.3 外来车辆 ExternalVehicleAdmin

- 自定义模板 `admin/external_vehicle_change_list.html`（月底清空倒计时）。
- Actions：CSV 导出 + `purge_external_vehicles_action`（一键清空全部外来车辆）。

### 8.4 进出记录 EntryExitRecordAdmin

- `save_model` 显式执行自动关联逻辑（与模型 `save()` 一致）。
- `list_display`：车牌号、车辆来源、进出类型（🟢/🔴）、时间、驾驶员、事由截断、同行人数、登记时间。
- `vehicle_source` 显示所属人/外来标识。

### 8.5 访客预约 VisitorAppointmentAdmin

- `list_display`：访客、电话、车牌、被访人/部门、预计时间、状态（🟡🟢🔴🔵⚫）、审批人、审批消息、创建时间。
- Actions：`✅ 审批通过` / `❌ 拒绝`（批量更新状态并记录审批人）。
- 支持按状态/时间/部门筛选。

### 8.6 非机动车 NonMotorVehicleAdmin

- 编号牌、类型、所属人、电话、创建时间；编号牌排序；CSV 导出。

### 8.7 校园地点 CampusLocationAdmin（campus_nav）

- 自定义模板 `admin/campus_nav/change_list.html`：通过高德地图点击选点，`fetch` 写入 `longitude/latitude`。
- 展示坐标（6 位小数）、名称、创建时间。

---

## 九、前端 SPA（frontend/）

### 9.1 路由（router/index.js）

| 路径 | 页面 | 说明 |
|------|------|------|
| `/login`、`/register` | Login / Register | 游客访问；已登录自动跳首页 |
| `/` | Home | 需登录；预约申请 / 申请状态 / 校内导航三栏 |
| `/qr/:id` | QRPass | 需登录；展示并下载已审批预约的二维码 |
| `/nav` | Navigation | 需登录；校内地点列表 |
| `/nav/:id` | NavigationDetail | 需登录；高德地图导航 |

### 9.2 认证

- `stores/auth.js`（Pinia）：`login` / `register` 调用 API 后把 token 存入 `localStorage`，并写入 axios 默认请求头 `Authorization: Token <token>`；`fetchUser`、`logout`、`init`。
- `api/index.js`：axios 实例，`baseURL: '/api'`，请求拦截器自动附加 Token。
- 路由守卫：未登录访问需登录页跳转 `/login`。

### 9.3 页面功能

- **Home**：三栏面板 —— 提交访客预约（含 `datetime-local` 预计时间）、查看本人预约状态（已通过显示 QR 入口）、校内导航地点列表。
- **QRPass**：获取二维码图片（blob）与有效期，支持保存 PNG。
- **NavigationDetail**：高德地图步行导航 —— 目的地标记、GPS 定位、路线规划、转向步骤高亮、语音播报（`speechSynthesis`）、校园地图弹窗（`/static/map/campus_map.jpg`）。

### 9.4 样式

- `assets/tesla.css` 定义 Tesla 风格 CSS 变量（蓝色 `#3E6AE1`、碳黑 `#171A20` 等），全站统一。
- Admin 侧使用 `static/css/admin_tesla.css`，设计参考见 `DESIGN-tesla.md`。

---

## 十、校园导航（apps/campus_nav）

- 模型 `CampusLocation`：`name`（地点名称）、`longitude`、`latitude`、`created_at`，按名称排序。
- `CampusLocationViewSet`：`IsAuthenticatedOrReadOnly`，读取公开、写入需认证；路由挂载于 `/api/admin/locations/`。
- 高德地图凭据（Key / 安全密钥）作为常量写在：
  - `frontend/src/views/NavigationDetail.vue`（SPA 导航）
  - `templates/admin/campus_nav/change_list.html`（Admin 地点选择器）
  - 配置及 `static/map/campus_map.jpg` 说明见 `static/map/README.md`。

---

## 十一、Excel 批量导入（views.py）

### 11.1 操作流程

1. 在"在校车辆档案"管理页点击"下载导入模板"（`/admin/download-template/`）。
2. 按模板格式填写车辆信息。
3. 在"在校车辆档案"管理页点击"导入 Excel"（`/admin/import-excel/`）上传文件。
4. 系统解析并给出成功/跳过/失败统计。

### 11.2 Excel 表头格式

| 车牌号（必填） | 车辆类型 | 品牌 | 颜色 | 所属人/部门（必填） | 联系电话 |
|---------------|---------|------|------|-------------------|---------|
| 京A12345 | 教职工车辆 | 大众 | 白色 | 张老师 | 13800001111 |

### 11.3 车辆类型映射

| 中文 | 数据库值 |
|------|---------|
| 校车 | school_bus |
| 教职工车辆 / 教职工 | staff |
| 公务车辆 / 公务 | official |
| 访客车辆 / 访客 | visitor |
| 送货车辆 / 送货 | delivery |

- 自动跳过已存在的车牌号；未识别类型默认 `staff`。

---

## 十二、模板（templates/，共 7 个自定义模板）

| 模板文件 | 用途 |
|---------|------|
| `admin/base_site.html` | 全局扩展：引入 `admin_tesla.css`、修改站点标题 |
| `admin/stats_dashboard.html` | 统计看板：9 张数据卡片 + 最近进出记录 |
| `admin/external_vehicle_change_list.html` | 外来车辆月底清空倒计时条 |
| `admin/school_vehicle_change_list.html` | 校内车辆黑名单提醒条 |
| `admin/import_excel.html` | Excel 批量导入页 |
| `admin/campus_nav/change_list.html` | 校园地点高德地图选点 |
| `visitor_appointment.html` | 访客自助预约公开页 |

### 12.1 统计看板

9 张卡片：今日进校 / 今日出校 / 当前在校 / 在校车辆 / 非机动车 / 本月外来 / 待审批访客 / 今日到访 / 黑名单车辆；下方为最近 10 条进出记录。CSS Grid 布局，响应式适配。

### 12.2 外来车辆清空倒计时

继承 `admin/change_list.html`，顶部插入渐变色倒计时条：

- 背景：`linear-gradient(135deg, #f1e251, #eacf5c)`（黄）
- 倒计时数字 `font-size: 22px; font-weight: 700`，每秒刷新
- 到期后显示"已到期，请清空！"
- 提供 `⚠ 立即清空所有外来车辆记录` action

### 12.3 校内车辆黑名单提醒

在校内车辆列表页顶部显示黑名单车辆总数提醒条，并提供"查看黑名单车辆"快捷链接。

---

## 十三、管理命令

### 13.1 播种测试数据 — `python manage.py seed_data`

创建：3 辆在校车（含 1 辆黑名单）、4 条进出记录（其中京Z99999 复用进出，自动创建 1 辆外来车）、2 条访客预约。

> 车辆和预约用 `get_or_create`，但进出记录每次运行都会新建，需保持数据干净时勿重复运行。

### 13.2 清空外来车辆 — `python manage.py purge_external_vehicles`

删除 `ExternalVehicle` 全部记录；关联进出记录外键 `SET_NULL`，进出记录保留。建议每月月初执行。

---

## 十四、静态资源与媒体

- 源静态资源在 `static/`；`STATIC_ROOT` 为 `staticfiles/`，由 `python manage.py collectstatic` 更新。
- `static/map/campus_map.jpg` 为校园地图占位图，可替换为实际地图。
- 媒体文件上传目录为 `uploads/`，经 `/media/` 访问。

---

## 十五、测试

```bash
python manage.py test apps.vehicle_mgmt.tests.AdminPageTests \
                       apps.vehicle_mgmt.tests.AutoLinkTests \
                       apps.vehicle_mgmt.tests.PurgeCommandTests \
                       apps.vehicle_mgmt.tests.NonMotorVehicleTests
```

**测试覆盖：27 项全部通过**

| 测试类 | 数量 | 覆盖内容 |
|--------|------|----------|
| AdminPageTests | 11 | 所有 Admin 列表页、搜索、筛选、黑名单、统计看板 |
| AutoLinkTests | 5 | 在校关联、外来创建、去重、在校优先、owner_name |
| PurgeCommandTests | 1 | 外来车辆清空命令 |
| NonMotorVehicleTests | 10 | 合法/非法编号牌、重复拦截、Admin 可访问、统计展示 |

---

## 十六、开发命令

后端（仓库根目录）：

```bash
python manage.py migrate
python manage.py check
python manage.py test
python manage.py runserver 8001
```

前端（`frontend/` 下，先 `npm ci` 安装依赖）：

```bash
npm run dev
npm run build
```

- Vite 监听 `5173`，并把 `/api`、`/media`、`/static` 代理到 `127.0.0.1:8001`；调试 SPA 时后端用 `runserver 8001`。
- 前端无 lint/typecheck/test 脚本，验证用 `npm run build`；后端验证用 `check`/`test`。
- 修改模型后先 `makemigrations` 再 `migrate`。
- 首次使用需创建数据库 `overclassproject` 并 `python manage.py createsuperuser`。

---

## 十七、用户角色权限

| 角色 | 权限范围 |
|------|----------|
| 管理员 | 外来车辆、校内车辆、访客预约、非机动车、校园地点、用户组、用户管理 |
| 工作人员 | 外来车辆、校内车辆、访客预约、非机动车 |

> 当前版本所有用户均为超级管理员；角色权限控制可通过 Django 内置 Group + Permission 体系实现。
> SPA 普通用户通过 `/register` 注册，仅能操作自己的预约（DRF `AppointmentViewSet` 按 `user` 过滤）。
